export enum Goal { PERFORMANCE = 'Rendimiento' } export enum UserLevel { BEGINNER = 'Principiante' } export type UserRole = 'coach' | 'client' | 'owner'; export interface User { id: string; name: string; email: string; role: UserRole; }