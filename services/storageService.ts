export const storageService = { getUser: () => JSON.parse(localStorage.getItem('kinetix_user') || 'null'), saveUser: (u) => localStorage.setItem('kinetix_user', JSON.stringify(u)), getSystemConfig: () => ({ enableAI: true }), getStorageUsage: () => ({ usedKB: 0, percentage: 0 }) };